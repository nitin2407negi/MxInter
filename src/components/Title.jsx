import React from "react";

const Title = () => {
  return <h1 style={styles.title}>Science Fiction Stories</h1>;
};

const styles = {
  title: {
    fontSize: "40px",
    margin: "20px 0",
  },
};

export default Title;